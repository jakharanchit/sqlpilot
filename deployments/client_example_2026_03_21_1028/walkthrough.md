# Deployment Walkthrough
**Client:** client_example  
**Date:** 2026-03-21 10:28:39

---

## What We're Doing Today

These changes improve database query performance.

### Changes being applied:

- Optimized **Labels, Parts, TestRequests, Tests, ItemLabels** — performance improvement applied
- Optimized **Labels, Parts, TestRequests, Tests, ItemLabels** — performance improvement applied

**Estimated time:** 15–20 minutes  
**Risk level:** Low — a rollback script is ready if anything looks wrong

---

## Before We Start

✅ Complete the **pre_flight.md** checklist first.

---

## Step 1 — Open SQL Server Management Studio (SSMS)

1. Open SSMS on the database server machine
2. Connect to server: `localhost`
3. In the top toolbar, confirm the selected database is: **VEXA**
   - If not: click the dropdown and select `VEXA`
4. Open a new query window (Ctrl+N)
5. Run this quick check:
   ```sql
   SELECT DB_NAME()
   ```
   ✅ Should return: `VEXA`

---

## Step 2 — Open and Run deploy.sql

1. In SSMS: File → Open → File...
2. Navigate to this folder: `client_example_2026_03_21_1028`
3. Open: **deploy.sql**
4. Press **F5** (or click Execute)
5. Watch the Messages tab at the bottom

✅ Expected output:
```
Starting deployment on: VEXA
Applying migration 001: optimize: SELECT TOP (1000) [testRequestID]...
Migration 001 complete.
Applying migration 002: optimize: SELECT TOP (1000) [testRequestID],[testRequestDate...
Migration 002 complete.
All migrations applied successfully.
```

⚠ If you see any **red error messages** — stop here and run rollback.sql (Step 5).

---

## Step 3 — Verify Everything Works

1. Open your application (LabVIEW / dashboard)
2. Test the filters and screens that were previously slow
3. Confirm they respond noticeably faster

**Expected improvements:**


---

## Step 4 — Confirm Completion

Once everything looks good:

1. Note the finish time in pre_flight.md
2. Let the developer know deployment is confirmed:
   > *'Deploy completed successfully at [time]. Application tested and working.'*

---

## Step 5 — If Anything Looks Wrong (Rollback)

**If anything is not working correctly after deployment:**

1. In SSMS: File → Open → File...
2. Open: **rollback.sql** (from the same folder)
3. Press **F5**
4. Wait for: `Rollback complete. Database restored to previous state.`
5. Restart the application and confirm it works as before
6. Contact the developer: *'Rollback completed at [time].'*

✅ The rollback script is safe to run — it will not delete any data.
   It only removes the indexes and view changes that were added today.

---

## Summary

| File | Purpose |
|------|---------|
| `deploy.sql` | Apply all changes |
| `rollback.sql` | Undo all changes |
| `pre_flight.md` | Checklist before starting |
| `technical_report.md` | Full technical details |
| `session_log.txt` | Auto-written during deployment |

---
*Generated by SQL Optimization Agent · 2026-03-21 10:28:39*